// Get a randomo value between 0 and 11 to select a musical key
Math.floor(Math.random() * 12);

// To stop repeating keys 11 can becaome the size of the array holding the musical keys, 
// then, after each execution pop the selected value